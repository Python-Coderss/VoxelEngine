#version 430 core

/**
 * Light Propagation Compute Shader (Static).
 * Used for calculating light levels across the voxel grid on the GPU.
 */

layout(local_size_x = 8, local_size_y = 8, local_size_z = 8) in;

// Placeholder for future GPU-accelerated lighting logic
void main() {
    // Current light propagation is handled on the CPU in LightPropagationEngine.java
}
